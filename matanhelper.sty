\ProvidesPackage{matanhelper}

% Теоремы, следствия, замечания, ...
\theoremstyle{plain}
\newtheorem{theorem}{Теорема}[chapter]
\newtheorem{theorem-non}{Предложение}[chapter]
\newtheorem{statement}{Утверждение}[chapter]
\newtheorem{lemma}{Лемма}[chapter]
\newcounter{corollaryCounter}[theorem]
\newtheorem{corollary}[corollaryCounter]{Следствие}
\newtheorem*{corollary*}{Следствие}

\theoremstyle{definition}
\newtheorem*{example}{Пример}
\newtheorem*{examples}{Примеры}
\newtheorem*{exercise}{Упражнение}
\newtheorem{definition}{Определение}[chapter]

\theoremstyle{remark}
\newtheorem*{remark}{Замечание}
\newtheorem*{editremark}{Замечание (от редакторов конспекта)}
\newtheorem*{remarks}{Замечания}
\newtheorem*{exerciseAnswer}{Ответ на упражнение}

% Нумеровать формулы по главам
\numberwithin{equation}{chapter}

% Числовые множества
\newcommand{\R}{\mathbb{R}}
\renewcommand{\C}{\mathbb{C}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\Q}{\mathbb{Q}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\T}{\mathbb{T}}

% Поднятая chi
\DeclareRobustCommand{\rchi}{{\mathpalette\irchi\relax}}
\newcommand{\irchi}[2]{\raisebox{\depth}{$#1\chi$}} % inner command, used by \rchi

% Специальная команда обрамления в кружочек
\newcommand*{\encircle}[1]{%
  \relax%
  \ifmmode%
    \mathpalette\@encircle@math{#1}%
  \else%
    \@encircle{#1}%
  \fi%
}
\newcommand*{\@encircle@math}[2]{%
  \@encircle{$\m@th#1#2$}%
}
\newcommand*{\@encircle}[1]{%
  \tikz[baseline,anchor=base] {%
    \node[draw,circle,outer sep=0pt,inner sep=.1ex] {#1};%
  }%
}

% Буквенные \Re и \Im
\DeclareMathOperator{\@custom@Re}{Re}
\DeclareMathOperator{\@custom@Im}{Im}
\renewcommand{\Re}{\@custom@Re}
\renewcommand{\Im}{\@custom@Im}

% Наклонные \le и \ge
\renewcommand{\le}{\leqslant}
\renewcommand{\ge}{\geqslant}
\renewcommand{\leq}{\leqslant}
\renewcommand{\geq}{\geqslant}

\def\lan{\left\langle }
\def\ran{\right\rangle}

% Математические операторы
\DeclareMathOperator{\Hom}{Hom}
\DeclareMathOperator{\Ker}{Ker}
\DeclareMathOperator{\rk}{rk}
\DeclareMathOperator{\Lin}{Lin}
\DeclareMathOperator{\GL}{GL}
\DeclareMathOperator{\SL}{SL}
\DeclareMathOperator{\sgn}{sgn}
\DeclareMathOperator{\sign}{sign}
\DeclareMathOperator{\ord}{ord}
\DeclareMathOperator{\id}{id}
\DeclareMathOperator{\St}{St}
\DeclareMathOperator{\tor}{tor}
\DeclareMathOperator{\End}{End}
\DeclareMathOperator{\chr}{char}
\DeclareMathOperator{\diag}{diag}

\newcommand{\GCD}{\text{НОД}}
\newcommand{\LCM}{\text{НОК}}

% Большие римские цифры
\makeatletter
\newcommand*{\rom}[1]{\expandafter\@slowromancap\romannumeral #1@}
\makeatother

\def\ds{\displaystyle}
\newcommand{\rmk}[1]{\noindent {\textbf{#1}}}

% Биг сабсет
\newcommand\bigsubset[1][1.19]{%
   \mathrel{\vcenter{\hbox{\scalebox{#1}{$\subset$}}}}}

% Биг сапсет
\newcommand\bigsupset[1][1.19]{%
   \mathrel{\vcenter{\hbox{\scalebox{#1}{$\supset$}}}}}

% Как vdots только более удобные для использованияя как знак "делится на"
\newcommand{\dvdots}{\mathop{\raisebox{-2pt}{\vdots}}}

% Лефт мапсто
\newcommand\leftmapsto{\mathrel{\reflectbox{\ensuremath{\mapsto}}}}
